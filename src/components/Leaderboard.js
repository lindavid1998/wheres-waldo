import React from 'react'
import styled from 'styled-components/macro';

const Header = styled.h1`
	font-size: 1rem;
`;

const Leaderboard = () => {
  return <Header>Leaderboard</Header>;
}

export default Leaderboard